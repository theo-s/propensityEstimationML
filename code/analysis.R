library(xgboost)
library(Rforestry)


source("code/helpers_analysis.R")


# x <- iris[,2:4]
# y <- iris[,1]
#
# booster <- xgb_helper(Xobs = x,
#                       Yobs = y,
#                       tune_length = 50)
#
# preds <- xgb_predict(booster, x)
